export type MatchRequest = {
  from_user: string;
  to_user: string;
  id: number;
  name: string;
  age: number;
  from: string;  
  to: string;
  date: string;
  photoUrl: string;
  status?: string; 
};  